server {
    listen 80;
    server_name localhost;

    # Serve the React application
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # SPA routing fallback
        try_files $uri $uri/ /index.html;
    }

    # Reverse proxy for API calls (optional, if frontend needs to bypass CORS)
    # location /api/ {
    #     proxy_pass http://backend:8001/; # Replace 'backend' with the actual backend service name or IP
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    # }

    # Reverse proxy for go2rtc (optional, if frontend needs to bypass CORS)
    # location /go2rtc/ {
    #     proxy_pass http://go2rtc:1984/; # Replace 'go2rtc' with the actual go2rtc service name or IP
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     
    #     # WebSocket support
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection "upgrade";
    # }
}
